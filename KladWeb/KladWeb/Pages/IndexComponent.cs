using System;
using System.Diagnostics;
using System.Linq;
using System.Threading.Tasks;
using Blazor.Extensions;
using Microsoft.AspNetCore.Components;
using Microsoft.JSInterop;
using Treasure.GameLogic.State.Field;

namespace KladWeb.Pages
{
    public class IndexComponent : ComponentBase
    {
        
        /*
        protected ElementReference TextureAtlasImage;
        protected BECanvasComponent CanvasReference;
        private readonly Random _rnd = new Random();

        protected async Task OnMakeField()
        {
            var @params = new GameFieldParams(10, 10, 2, 3, 6, 3, 0.1, false);
            var field = new GameFieldGenerator(@params, new Random()).Generate();
            
            await new TreasureFieldRenderer(CanvasReference, field, TextureAtlasImage).RenderAsync();
            
            Console.WriteLine("Meh");
        }*/
    }
}
